[Unit]
Description=Open source RGB lighting control that doesn't depend on manufacturer software.
After=graphical.target

[Service]
Type=simple
StandardOutput=journal
ExecStart=openrgb --server --loglevel 2
Restart=on-failure

[Install]
WantedBy=default.target
